# Direct runtime dependencies for the local read-only VALORANT tracker backend.
# Versions are pinned to the environment used for this cleanup.
Flask==3.1.3
flask-cors==6.0.3
requests==2.34.2
python-dotenv==1.2.2
urllib3==2.7.0
